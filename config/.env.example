# Exemplo de variáveis de ambiente para configuração SMTP
# Copie este arquivo para `config/.env` (ou defina as variáveis no sistema)

SMTP_HOST=smtp.gmail.com
SMTP_USERNAME=seu-email@gmail.com
SMTP_PASSWORD=SUA_SENHA_DE_APP_AQUI
SMTP_PORT=587
SMTP_ENCRYPTION=tls

MAIL_FROM_EMAIL=reservas@seudominio.com
MAIL_FROM_NAME="Nome do Hotel"
